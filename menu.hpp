#ifndef MENU_HPP
#define MENU_HPP

#include "rwFunc.hpp"
#include <string>

int displayData(InfoAboutLand *land, int currentRecord);
int menu(InfoAboutLand *land, std::string FILE);

#endif // MENU_HPP
